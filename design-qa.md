# Design QA

final result: passed

Reference: Product Design option 3, Perturbation Console, generated in
`/Users/williamblair/.codex/generated_images/019f3e72-18bf-7e03-b0fc-33bac4847f15/ig_0eb14d81b8c6a896016a4fd0f5dddc81958a2b636f38cb7233.png`.

Prototype checked: `http://localhost:8125/`.

## Result

- The old left rail shell is removed. The app now opens with a top workflow console.
- The first screen leads with the Claude Science signature and Prospect driver/passenger split.
- The selected direction's assay-console idea is present through the perturbation matrix: gene rows,
  Rest, Stim8hr, Stim48hr columns, and a final typed-status column.
- Palette is retuned to lab glass, steel, clinical blue, teal driver evidence, and vermilion
  contradicted claims.
- Browser DOM QA confirmed no sidebar node, no old named aesthetic in rendered text, no desktop
  overflow, and no mobile page-level horizontal overflow at 390px.
- Build passed with `cd web && npm run build`.

## Remaining P3 Polish

- The matrix could become the dominant first-screen object in a later pass by moving it above the
  verdict examples.
- The Receipts tab still intentionally contains protocol detail, but it no longer defines the shell.
